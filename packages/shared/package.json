{
  "name": "@app/shared",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "UNLICENSED",
  "dependencies": {
    "zod": "^3.23.8"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --outDir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint in shared yet'",
    "dev": "npm run build -- --watch",
    "clean": "rimraf dist || true"
  }
}
